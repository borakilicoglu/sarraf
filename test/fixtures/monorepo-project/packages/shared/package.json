{
  "name": "@acme/shared",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "rimraf build",
    "build": "tsdown --config-loader unrun",
    "check:exports": "attw --pack .",
    "typecheck": "tsc --noEmit -p tsconfig.build.json",
    "test": "vitest run"
  }
}
